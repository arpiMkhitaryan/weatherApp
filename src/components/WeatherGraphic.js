import React, { Component } from 'react';

import { appleStock } from '@vx/mock-data';

export default class WeatherGraphic extends Component{
    render(){
        const data = appleStock;
        // console.log(data)
        return(
            <div>abracadabra</div>
        )
    }
}